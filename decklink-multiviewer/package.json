{
  "name": "decklink-multiviewer",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone Windows output app. Renders the OBTV multiviewer (via a public share link) and sends it to a Blackmagic DeckLink Duo 2 as 1080i59.94 SDI with embedded audio. Fully isolated from the web app.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "rebuild-addon": "node-gyp rebuild -C native"
  },
  "devDependencies": {
    "electron": "^31.7.6",
    "node-gyp": "^10.2.0"
  },
  "dependencies": {
    "node-addon-api": "^8.2.1"
  }
}
